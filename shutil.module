import shutil
import os
shutil.move
shutil.copy
shutil.copy2
shutil.copytree
shutil.rmtree


for i in range(1, 101):
    # os.mkdir(f"tutorial{i}")
    shutil.copy("main.txt",f"tutorial{i}")
    # shutil.rmtree(f"tutorial{i}")
